#set( $pound = '#' )
#set( $dollar = '$' )
#set( $escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>

<datasources>
    <local-tx-datasource>
        <jndi-name>${rootArtifactId}Datasource</jndi-name>
        <use-java-context>true</use-java-context>
        <connection-url>${dollar}{db.unit.connection.url}</connection-url>
        <driver-class>${dollar}{db.unit.driver.class}</driver-class>
        <user-name>${dollar}{db.unit.user.name}</user-name>
        <password>${dollar}{db.unit.password}</password>
    </local-tx-datasource>
</datasources>
